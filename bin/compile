#!/usr/bin/env bash

set -e

mkdir -p "$1/bin/"
cp "bin/pdftotext-${STACK}" "$1/bin/pdftotext"
cp "bin/pdftoppm-${STACK}" "$1/bin/pdftoppm"
echo "-----> pdftotext-buildpack: Installed pdftotext and pdftoppm to app/bin"
exit 0
